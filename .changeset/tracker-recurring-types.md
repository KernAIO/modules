---
'@kernhq/module-tracker': patch
---

Export `RecurrenceRule`, `RecurringIssue` and `IssueTemplate` from `@kernhq/module-tracker/client`,
so an interface can name the schedules and templates it shows.
