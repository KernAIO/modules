{
  "name": "@kernhq/modules-workspace",
  "private": true,
  "version": "0.0.0",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "format": "biome format --write .",
    "changeset": "changeset",
    "release": "pnpm build && changeset publish",
    "note": "echo \"Every module moved to its own repository \u2014 see README.md\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@changesets/cli": "^2.29.0",
    "@kernhq/tsconfig": "^0.1.0",
    "turbo": "^2.10.0",
    "typescript": "~5.9.3",
    "vitest": "^4.0.0"
  }
}
